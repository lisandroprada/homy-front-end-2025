import Compare from '@/components/inner-pages/compare';
import Wrapper from '@/layouts/Wrapper';

export const metadata = {
  title: 'Compare Homy - Propietas Inmobiliaria',
};
const index = () => {
  return (
    <Wrapper>
      <Compare />
    </Wrapper>
  );
};

export default index;
